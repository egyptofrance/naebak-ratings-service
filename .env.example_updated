# إعدادات خدمة التقييمات الذكية - مشروع نائبك

# إعدادات Django الأساسية
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# إعدادات قاعدة البيانات
DB_NAME=naebak_ratings
DB_USER=postgres
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_PORT=5432

# إعدادات JWT
JWT_SECRET_KEY=your-jwt-secret-key
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_LIFETIME=1
JWT_REFRESH_TOKEN_LIFETIME=7

# إعدادات Redis
REDIS_URL=redis://localhost:6379/0

# إعدادات البريد الإلكتروني
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-email-password
DEFAULT_FROM_EMAIL=noreply@naebak.com

# إعدادات نظام التقييمات
ENABLE_RATINGS=True
ENABLE_COMMENTS=True
REQUIRE_LOGIN=True

# حدود التقييم
MAX_RATINGS_PER_USER_PER_DAY=10
MIN_RATING_INTERVAL_MINUTES=5
MAX_COMMENT_LENGTH=500

# إعدادات التقييمات الذكية
DEFAULT_DISPLAY_MODE=mixed
DEFAULT_FAKE_RATING=4.5
DEFAULT_FAKE_COUNT=1000
DEFAULT_REAL_WEIGHT=0.3
DEFAULT_FAKE_WEIGHT=0.7

# إعدادات الأمان
ENABLE_IP_TRACKING=True
BLOCK_DUPLICATE_IPS=True
AUTO_VERIFY_RATINGS=False
MODERATE_COMMENTS=True

# إعدادات الإشعارات
NOTIFY_ON_NEW_RATING=True
NOTIFY_ON_REPORT=True

# إعدادات مراجعة المحتوى
ENABLE_AUTO_MODERATION=True
BLOCK_OFFENSIVE_LANGUAGE=True
PROFANITY_FILTER_ENABLED=True
SPAM_DETECTION_ENABLED=True

# إعدادات التحليلات
ENABLE_ANALYTICS=True
UPDATE_STATS_INTERVAL_MINUTES=60
CACHE_STATS_DURATION_SECONDS=3600

# إعدادات Celery (اختيارية)
USE_CELERY=False
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# إعدادات التخزين السحابي (اختيارية)
USE_CLOUD_STORAGE=False
GS_BUCKET_NAME=naebak-ratings
GS_PROJECT_ID=your-gcp-project-id
GS_CREDENTIALS=path/to/credentials.json

# إعدادات المراقبة (اختيارية)
SENTRY_DSN=your-sentry-dsn

# إعدادات الخدمات الأخرى
AUTH_SERVICE_URL=http://localhost:8001
COMPLAINTS_SERVICE_URL=http://localhost:8002
MESSAGING_SERVICE_URL=http://localhost:8003
CONTENT_SERVICE_URL=http://localhost:8005
ADMIN_SERVICE_URL=http://localhost:8006

# إعدادات التقييمات الذكية المتقدمة
AUTO_CREATE_SMART_RATINGS=True
BULK_UPDATE_BATCH_SIZE=100
RECALCULATE_INTERVAL_HOURS=24

# إعدادات تنظيف البيانات
AUTO_DELETE_OLD_RATINGS=False
OLD_RATING_RETENTION_DAYS=365
AUTO_DELETE_REPORTED_RATINGS_DAYS=90

# إعدادات الميزات
ENABLE_SMART_RATINGS=True
ENABLE_RATING_ANALYTICS=True
ENABLE_BULK_OPERATIONS=True
ENABLE_RATING_REPORTS=True

# إعدادات التطوير
ENABLE_DEBUG_TOOLBAR=False
ENABLE_SILK_PROFILING=False
